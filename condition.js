var level = 1;
if(level % 2 == 0){ // %2 is used to check the number for either even or odd. It finds the remainder of the product. Even #'s have 0 remainder, while odd #'s don't 
    console.log("You're on an even level!");
}
else{
    console.log("You're on an odd level!");
}